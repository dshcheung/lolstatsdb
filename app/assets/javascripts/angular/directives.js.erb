app.directive('mainNavBar', function(){
  return {
    restrict: 'E',
    templateUrl: "<%=  asset_path('partials/mainNavBarPartial.html') %>",
  };
});

app.directive('subNavBar', function(){
  return {
    restrict: 'E',
    templateUrl: "<%=  asset_path('partials/subNavBarPartial.html') %>",
  };
});
