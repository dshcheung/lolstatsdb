app.directive('mainNavBar', function(){
  return {
    restrict: 'E',
    templateUrl: "<%=  asset_path('partials/mainNavBarPartial.html') %>",
  };
});

app.directive('subNavBar', function(){
  return {
    restrict: 'E',
    templateUrl: "<%=  asset_path('partials/subNavBarPartial.html') %>",
  };
});

app.directive('profileTab', function(){
  return {
    restrict: 'E',
    templateUrl: "<%=  asset_path('partials/profileTabPartial.html') %>"
  };
});

app.directive('runesTab', function(){
  return {
    restrict: 'E',
    templateUrl: "<%=  asset_path('partials/runesTabPartial.html') %>"
  };
});

app.directive('championsTab', function(){
  return {
    restrict: 'E',
    templateUrl: "<%=  asset_path('partials/championsTabPartial.html') %>"
  };
});
