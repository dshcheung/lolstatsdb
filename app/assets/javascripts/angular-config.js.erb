var app = angular.module('lolstatsdb', ['ngRoute'])

app.config(['$routeProvider',function($routeProvider) {
	$routeProvider.when('/',{
		templateUrl: "<%=  asset_path('welcome.html') %>",
		controller: "WelcomeCtrl"
	})
}]);
